# default platform configuration - always loaded first, override values in .env
# name of the platform (project): container/volume names and the access hosts
# lab.<COMPOSE_PROJECT_NAME>.localhost / traefik.<COMPOSE_PROJECT_NAME>.localhost
COMPOSE_PROJECT_NAME=stellars-jupyterlab-ds
# external HTTPS port the platform listens on
LAB_PORT=443
