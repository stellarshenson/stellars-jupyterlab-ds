# ----------------------------------------------------------------------------------------
#
#   Jupyterlab setup with NVIDIA GPU support
#
#   Jupyter Lab - http://localhost:8888
#   Tensorboard - http://localhost:6006
#
# ----------------------------------------------------------------------------------------
services:
  # run jupyterlab on a local port 8888
  # without any authentication
  jupyterlab:
    container_name: jupyterlab-nvidia
    hostname: jupyterlab-nvidia
    image: stellars/stellars-jupyterlab-ds:latest
    build:
       context: build
       dockerfile: Dockerfile
    command: /start-jupyterlab.sh
    environment:
       - GPU_SUPPORT_ENABLED=1
       - JUPYTERLAB_SERVER_IP=*
       - JUPYTERLAB_SERVER_TOKEN=
       - JUPYTERLAB_SERVER_PASSWORD=
    ports:
       - 8888:8888 # jupyterlab
       - 8000:8000 # jupyterhub
       - 8001:8001 # jupyterhub proxy
       - 6006:6006 # tensorboard
       - 5000:5000 # mlflow
    volumes:
      - home:/home/lab           # jupyterlab user directory persistence
      - workspace:/mnt/workspace # user projects workspace volume persistence
      - certs:/mnt/certs         # persistent certificate store for ssl
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  workspace:
  certs:
  home:

# EOF

