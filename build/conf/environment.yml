name: base
channels:
  - conda-forge
  - rapidsai
  - nvidia
  - defaults

dependencies:
  - python=3.10

  # nvidia packages
  - cudatoolkit
  - rapids
  - cudf
  - cuml
  - cugraph

  # other useful libs
  - colorama
  - pandas
  - numba
  - numpy
  - git
  - make
  - matplotlib-inline
  - nodejs
  - pip

  # jupyterlab and language server
  - jedi-language-server
  - jupyterlab-git
  - jupyterlab-lsp
  - jupyterlab>=4.0.0

  - pip:
      - botocore
      - click
      - cookiecutter
      - dask
      - jupyterlab_favorites
      - matplotlib
      - memory-profiler
      - nbconvert[webpdf]
      - plotly
      - playwright
      - polars
      - py-cpuinfo
      - py-dotenv
      - pyarrow
      - s3fs
      - scikit-learn
      - scikit-learn-intelex
      - scipy
      - seaborn
      - tensorflow
      - tensorflow[and-cuda]
      - termcolor
      - tsfresh
      - theme-darcula
